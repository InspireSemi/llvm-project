# Thunderbird plugins-nextgen utils

## Getting started

In order to build the InspireSemi runtime infrastructure alongside LLVM, you 
must clone necessary repositories into the `utils` directory as follows:

```
cd llvm-project/offload/plugins-nextgen/thunderbird/utils
git clone git@github.com:InspireSemi/host-runtime.git DataTransferEngine
```
